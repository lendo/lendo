1。周例会，介绍分站上周任务，邓鹏休假，他的组未参加。
2。注意咪咕播放器的上传自身收藏的播放列表的功能，该功能是将用户的账户加密后作为文件名，存储一个
   该XML文件到Servlet所在的Web服务器，而目前有两台Web服务器，供apache进行自由选择，所以当第一次
   上传XML文件到服务器A，下一次下载的时候Apache选择服务器B，那么上传的播放列表和下载的播放列表就
   会出现不一致的情况，要不就同时往两台服务器传文件，要不就写死固定IP，始终在一台服务器上面保存
   播放列表。
3。